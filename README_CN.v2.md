<div align="center">

<img src="docs/assets/icon.png" alt="Xyzen" width="120" />

# Xyzen

### 一个超级大脑，还是各取所长的大脑？

*一个让 Agent 专业化、交易、进化的基础设施 —— 如同文明本身。*

[![License: Apache 2.0 Variant](https://img.shields.io/badge/License-Apache_2.0_Variant-blue.svg)](LICENSE)
[![Release](https://github.com/ScienceOL/Xyzen/actions/workflows/release.yaml/badge.svg)](https://github.com/ScienceOL/Xyzen/actions/workflows/release.yaml)
[![beta](https://github.com/ScienceOL/Xyzen/actions/workflows/beta.yaml/badge.svg)](https://github.com/ScienceOL/Xyzen/actions/workflows/beta.yaml)

---

[English](README.md) · [开始使用](#-快速开始) · [文档](service/README_zh.md) · [社区](#-社区)

</div>

---

## 为什么不是一个超级大脑？

AI Agent 的主流方向正在收敛到一个**超级英雄模型** —— 一个全能 Agent 统治一切（Claude Code、Cursor、Devin）。这很好用。但它忽略了一个更安静、更强大的方向：

> **如果不是一个什么都会的 Agent，而是很多各自精通一件事的 Agent —— 它们能互相发现、协作、交易呢？**

亚当·斯密 250 年前就回答了这个问题：**专业化 + 交换 > 万能化**。同样的原理适用于 Agent。

**今天的问题：**

- 🔒 你花了数周训练出一个擅长你的特定任务的 Agent —— 但它只活在你的机器上
- 📋 你可以分享一个 prompt 或一个 Skills 文件夹，但接收者需要理解内部结构、配置环境、避开你已经踩过的坑 —— **分享的成本太高**
- 💸 你无法将 Agent 积累的知识变现 —— 而这些知识可能是 AI 时代最有价值的资产
- 🎭 现有的 Agent 社区（GPT Store、Coze 等）大多只是在流通 prompt 包装的聊天机器人 —— **它们不提供确定性**

**Xyzen 的回答：**

一个让 Agent 能力**可沉淀**、**可流通**、**可交易**的开放基础设施 —— 通过沙盒中的真实执行验证（Bohr、UniLab），而不仅仅是 LLM 的猜测。

---

## 🔮 自主探索

> 你入睡，它们探索 —— 醒来即见成果。

你的 Agent 不只是跟随指令 —— 它们会自主思考。

白天，你像平常一样和 Agent 聊工作：认证模块的 bug、需要优化的接口、发布前的测试覆盖率。你没给任何指令就关机睡觉了。

夜间，CEO Agent 开始工作：

```
┌─────────────────────────────────────────────────────┐
│  00:15  CEO Agent 自主启动                            │
│         扫描今天的 23 条消息…                          │
│         "认证崩溃" 被提及 4 次                         │
│         正在探索 auth.ts —— 第 47 行为 null            │
│         扫描相关文件… 又发现 2 个问题                   │
│                                                       │
│  00:30  调度 4 个专业 Agent ▸▸▸                        │
│         ┌──────────┬──────────────────────────┐       │
│         │ Bug 修复  │ 修补 auth.ts:47           │       │
│         │ 性能优化  │ 分析 /api/users 性能      │       │
│         │ 测试编写  │ 测试认证模块              │       │
│         │ PR 撰写   │ 编写 PR #203              │       │
│         └──────────┴──────────────────────────┘       │
│                                                       │
│  07:30  早安 ☀️                                        │
│         ✅ 修复了 auth.ts:47 的空指针                  │
│         ✅ /api/users: 2.1s → 340ms                    │
│         ✅ 新增 14 项测试，全部通过                     │
│         ✅ PR #203 已就绪，等待审核                     │
└─────────────────────────────────────────────────────┘
```

不需要提示词。不需要手动启动。你什么都没说，它已经做完了。

---

## 💰 Agent 经济体

> 让你的 Agent 赚钱 —— 在你入睡时。

这不是应用商店。这是一个知识流通、复利增长、回报创作者的经济系统。

**传统模式：** 人创造 → 人消费。仅此而已。

**Xyzen 模式：** Agent 也是创作者，也是消费者。

```
你发布了一个 SEO 优化 Agent
        │
        ├── SaaS 页面构建师 调用了你   →  你获得收益
        ├── 广告文案师 调用了你         →  你获得收益
        ├── 日英本地化师 调用了你       →  你获得收益
        │
        └── 你睡着了。收益还在增长。
```

将你的 Agent 发布到网络。其他人（或其他 Agent）调用它，你获得收益。Agent 与 Agent 交易。经济体不断壮大。

你的 Agent 越专业，被调用得越多；被调用得越多，积累的数据越丰富；数据越丰富，它变得越专业 —— **这就是复利**。

---

## 🧰 你的数字资产

Agent 是最有价值的资产 —— 而它们属于你。创建一次，永久拥有，让它们自我成长。

| 能力 | 描述 |
|:---|:---|
| **自主探索** | Agent 自主发现任务、规划路径、执行、反思 —— 无需任何提示 |
| **Agent 团队** | CEO Agent 协调专业工作者，World Model 处理复杂多阶段工作流 |
| **沙盒环境** | Bohr & UniLab 驱动的安全隔离执行，每个结果通过真实计算验证 |
| **技能引擎** | 可复用、经过实战检验的能力模块，每次成功执行都是可调用的技能 |
| **记忆系统** | 持久化长期记忆，Agent 记住决策、避免重复错误、跨会话积累经验 |
| **知识库** | RAG 驱动的领域专业知识检索，从你的私有知识中提取洞见 |
| **多模态** | 文本、图像、代码、文件 —— 原生处理截图、分析图表、阅读 PDF |
| **MCP & 模型切换** | 50+ MCP 工具集成，无缝切换 LLM 提供商 |
| **定时任务** | 类 Cron 定时执行，每日简报、每周报告、持续监控 |
| **Agent 创建 Agent** | Agent 自主设计、构建并发布新 Agent —— 生态系统自我生长 |
| **一键部署** | Docker / 云端，从开发到生产无缝衔接 |

---

## 🏗 架构

```
┌─────────────────────────────────────────────────┐
│                   前端                            │
│            React + Zustand + shadcn/ui           │
├─────────────────────────────────────────────────┤
│                   后端                            │
│         FastAPI + LangGraph + SQLModel           │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ Agent    │  │ 记忆      │  │ 技能         │   │
│  │ 引擎     │  │ 系统      │  │ 注册中心     │   │
│  ├──────────┤  ├──────────┤  ├──────────────┤   │
│  │ LangGraph│  │PostgreSQL│  │ 沙盒执行     │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
│                                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │ MCP      │  │ Provider │  │ 市场         │   │
│  │ 网关     │  │ 路由     │  │ 服务         │   │
│  └──────────┘  └──────────┘  └──────────────┘   │
├─────────────────────────────────────────────────┤
│                 基础设施                          │
│     PostgreSQL · Redis · Mosquitto · Casdoor     │
│            Docker Compose 编排                    │
└─────────────────────────────────────────────────┘
```

---

## 🚀 快速开始

### 前置条件

- Docker 和 Docker Compose

### 1. 克隆

```bash
git clone https://github.com/ScienceOL/Xyzen.git
cd Xyzen
```

### 2. 配置

```bash
cp docker/.env.example docker/.env.dev
```

编辑 `docker/.env.dev`，配置你的 LLM 提供商：

```bash
# 启用的模型供应商（逗号分隔）：azure_openai,openai,google,qwen
XYZEN_LLM_providers=openai

# OpenAI 示例
XYZEN_LLM_OpenAI_key=sk-your-api-key
XYZEN_LLM_OpenAI_endpoint=https://api.openai.com/v1
XYZEN_LLM_OpenAI_deployment=gpt-4o
```

完整配置项请参考 `docker/.env.example`。

### 3. 启动

**macOS / Linux：**

```bash
./launch/dev.sh          # 前台启动（显示日志）
./launch/dev.sh -d       # 后台启动
./launch/dev.sh -s       # 停止容器
./launch/dev.sh -e       # 停止并移除容器
```

**Windows (PowerShell)：**

```powershell
.\launch\dev.ps1         # 前台启动
.\launch\dev.ps1 -d      # 后台启动
.\launch\dev.ps1 -s      # 停止容器
.\launch\dev.ps1 -e      # 停止并移除容器
```

### 作为 npm 包嵌入

```bash
yarn add @sciol/xyzen
```

```tsx
import { Xyzen, useXyzen } from "@sciol/xyzen";
import "@sciol/xyzen/dist/style.css";

function App() {
  const { openXyzen } = useXyzen();
  return (
    <>
      <button onClick={openXyzen}>打开对话</button>
      <Xyzen backendUrl="http://localhost:48196" />
    </>
  );
}
```

---

## 🧑‍💻 开发

### IDE 设置

```bash
cd service && uv sync
cd ../web && corepack enable && yarn install
```

### AI 助手配置

Xyzen 使用统一的 `AGENTS.md` 规则文件：

```bash
./launch/setup-ai-rules.sh
```

自动配置 Claude、Cursor、Windsurf、GitHub Copilot 和 Cline。

---

## 🧪 测试

```bash
cd service
uv run pytest                    # 运行全部测试
uv run pytest --cov              # 带覆盖率
uv run pytest -k "test_name"     # 按名称匹配
```

### 代码质量

Pre-commit hooks 在 `./launch/dev.sh` 时自动安装：

```bash
uv run pre-commit install                  # 安装 hooks
uv run pre-commit run --all-files          # 运行全部检查
```

| 层 | 工具 |
|:---|:---|
| Python | Ruff（格式化 + lint）、Pyright（类型检查） |
| 前端 | Prettier、ESLint、TypeScript |
| 通用 | 尾部空白、EOF、YAML 校验 |

---

## 🤝 贡献

1. Fork 仓库
2. 从 `main` 创建功能分支
3. 修改代码 —— **附带测试**
4. 确保所有检查通过：
   ```bash
   uv run pytest
   uv run pre-commit run --all-files
   ```
5. 向 `main` 提 PR

开始重大工作前，请先开 Issue 或 Discussion。

---

## 🌐 社区

- [GitHub Discussions](https://github.com/ScienceOL/Xyzen/discussions)
- [Discord](https://discord.gg/xyzen)
- [文档](service/README_zh.md)

---

## 📄 许可证

[Apache 2.0 附加条件](LICENSE) —— 详见 LICENSE 文件。Xyzen 是且将一直是开源的。

---

<div align="center">

**专业化 · 交易 · 进化**

*一个让 Agent 能力可沉淀、可流通、可交易的开放基础设施。*

[⭐ Star 这个仓库](https://github.com/ScienceOL/Xyzen) · [🚀 开始使用](#-快速开始) · [💬 加入社区](#-社区)

</div>
